# 2026-03-24

## 关键进展

- 完成第五轮并行并收口：
  - `neptune-gateway-swift@869458d`（SQLite 持久化 + retention）
  - `neptune-contracts@1fe7a5f`（契约与 retention fixture 对齐）
  - `neptune-inspector-h5@bc50040`（sources/metrics 面板）
  - `neptune-sdk-harmony@7b60482`（`/v2/export/sources` + 来源去重）
- 完成第六轮“成熟库优先”并行审计与改造：
  - `neptune-gateway-swift@0abfa0b`（SQLite C API 替换为 GRDB）
  - `neptune-sdk-ios@8ead91e`（成熟库审计文档）
  - `neptune-sdk-android@f7d524c`（手搓 JSON 编码替换为 Jackson）
  - `neptune-sdk-harmony@ae7c7c3`（成熟库审计文档）
  - `neptune-inspector-h5@d00f22e`（协议边界引入 `zod` 运行时校验）
  - `neptune-desktop-macos@b77ea36`（成熟库审计文档）
- 完成第七轮“成熟库硬切”：
  - `neptune-sdk-ios@5375a57`（本地 HTTP 导出服务从 FlyingFox 迁移到 Vapor）
  - `neptune-sdk-android@3e90f5f`（本地 HTTP 导出服务从 NanoHTTPD 迁移到 Ktor）
- 完成第八轮并行收口：
  - `neptune-sdk-web@eebad71`（Web SDK 实装：队列/批量/退避重试/metrics）
  - `neptune-gateway-swift@362f37c`（100,000 条压测门禁 + 并发 id 唯一性校验）
  - `neptune-desktop-macos@e0ea395`（本地 inspector dist 优先加载）
  - `neptune-contracts@7929a99`（compatibility matrix 全量对齐，web 已验证）
  - 父仓新增 `docs-linhay/scripts/run-all-checks.sh` 作为统一门禁编排
- 完成第九轮并行收口：
  - `neptune-sdk-harmony@ae08584`（`/v2/export/logs` 增加 `platform/appId/sessionId` 过滤）
  - `neptune-gateway-swift@ed1f0bc`（`format=text` 输出 + 事件驱动长轮询）
  - `neptune-sdk-web@59b55dd`（gateway discovery + DSN/baseURL 回退）
  - `neptune-desktop-macos@a47ed23`（bundle inspector 资源路径 + README 去 TODO）
  - 父仓门禁脚本已纳入 web：`docs-linhay/scripts/run-all-checks.sh`
- 完成第十轮并行收口：
  - `neptune-sdk-ios@550ad17`（可选 GRDB 持久化队列）
  - `neptune-sdk-android@ae7af29`（可选 SQLDelight 持久化队列）
  - `neptune-sdk-harmony@fd51a74`（官方存储后端骨架 + 持久化验证脚本）
- 完成第十一轮收口：
  - `neptune-sdk-harmony@551a558`（`ohpm` 官方源 + `hvigorw assembleHar` 构建通过）
- 完成第十二轮并行收口（CI 工程化）：
  - `neptune-gateway-swift@cbc1379`（GitHub Actions + 手动 perf gate 开关）
  - `neptune-inspector-h5@ec524b4`（GitHub Actions）
  - `neptune-sdk-web@3052c46`（GitHub Actions + `npm pack --dry-run`）
  - `neptune-sdk-ios@3f4af13`（GitHub Actions）
  - `neptune-sdk-android@a64f662`（GitHub Actions）
  - `neptune-contracts@e9ed055`（compatibility matrix 同步上述最新 commit）
- 完成第十三轮并行收口（CI 覆盖扩展）：
  - `neptune-desktop-macos@bd87606`（GitHub Actions：`swift build` + `swift test`）
  - `neptune-sdk-harmony@06faaf1`（GitHub Actions：默认校验 + 手动 Harmony build 开关）
  - `neptune-contracts@b63df3b`（GitHub Actions：OpenAPI/fixtures 语法校验）
  - 父仓 `4ee0c63`（`.github/workflows/integration.yml` 集成门禁入口）
- 新增父仓统一发布编排入口 `release-orchestrator.yml`，只负责通过 GitHub API 触发子仓 release workflow，不在父仓构建产物。
- 完成第二十轮并行收口（native demo 集成冒烟）：
  - `neptune-sdk-ios@a13249b`（补齐 smoke 脚本 toolchain 解析，优先 `xcrun --find swift`）
  - `neptune-sdk-android@7d8875e`（新增 `examples/smoke-demo`）
  - `neptune-sdk-harmony@cfd1da5`（新增 `scripts/demo-smoke.mjs`）
  - 父仓新增 `docs-linhay/scripts/smoke-demo-native.sh`（串行编排 iOS/Android/Harmony demo 冒烟）
- 三端 demo 冒烟复验通过：
  - iOS：`SMOKE_RESULT ok=true`
  - Android：`./gradlew smokeDemo` 输出 health/metrics/records 摘要
  - Harmony：`demo-smoke: ok`
- 完成第二十一轮并行收口（CI + 集成门禁增强）：
  - `neptune-sdk-ios@1bd1053`（CI 新增独立 smoke-demo job）
  - `neptune-sdk-android@ddfbf59`（root 预加载 Kotlin plugin `apply false`，消除多重加载 warning，并新增 smoke-demo job）
  - `neptune-sdk-harmony@cfd1da5`（CI 默认执行 `scripts/demo-smoke.mjs`）
  - 父仓：`run-all-checks.sh` 支持 `NEPTUNE_CHECK_NATIVE_SMOKE=1` / `NEPTUNE_CHECK_WEB_SMOKE=1`，`integration.yml` 同步 workflow_dispatch 开关
- 父仓全量门禁实测通过（含两类 demo 冒烟）：
  - `NEPTUNE_CHECK_NATIVE_SMOKE=1 NEPTUNE_CHECK_WEB_SMOKE=1 bash docs-linhay/scripts/run-all-checks.sh`
- 第二十二轮模拟器实测推进：
  - Android：本机已安装 `android-commandlinetools` + `android-platform-tools`，完成 `sdkmanager` 依赖拉取、AVD `Neptune_API_34` 创建、`installDebug` 安装与 Activity 拉起；`logcat` 抓到 `NeptuneSimulatorDemo` 业务日志。
  - Harmony：检测到目标 `127.0.0.1:5555`，`entry-default-unsigned.hap` 安装成功，但 `aa start` 被设备锁屏策略（开发者模式下不允许自动解锁）阻塞。
  - iOS：`neptune-sdk-ios` 已具备真实 simulator demo app 工程（`7c1df8d`），当前机器缺少可用 iOS runtime（`simctl list runtimes` 为空）导致无法完成启动验证。
- 已留存截图证据：
  - Android: `docs-linhay/screenshots/20260324/android/20260324-android-simulator-demo-after-v01.png`
  - Android: `docs-linhay/screenshots/20260324/android/20260324-android-simulator-demo-after-v02.png`
  - Harmony: `docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-demo-after-v01.png`
- 完成第二十二轮并行收口（真实 demo 工程）：
  - `neptune-sdk-ios@7c1df8d`：新增 `Examples/simulator-app`（Tuist iOS App），`scripts/simulator-demo.sh` 可一键生成/构建/安装/启动模拟器 App
  - `neptune-sdk-android@860b945`：新增 `examples/simulator-app` 独立 Android application demo（composite build，不影响主 SDK 根构建）
  - `neptune-sdk-harmony@cae2760`：新增 `entry` 可运行 demo 模块，支持 `assembleHap` 与 demo 结构校验脚本
- iOS 模拟器实测：`bash neptune-sdk-ios/scripts/simulator-demo.sh` 成功，`iPhone 17 Pro (iOS 26.2)` 上已完成 `simctl install` 与 `simctl launch`。

## 关键决策

- 全仓执行“成熟库优先”硬约束：SQLite / HTTP / CLI / 协议解析优先使用成熟库，不接受手搓基础设施实现。
- 对 Android 生态补充决策：`GRDB 7+` 作为 Swift-on-Android 路径可行，但当前 Kotlin/JVM SDK 主线优先 Ktor/Android 原生生态，避免跨语言桥接成本。
- v2 日志主链路保持 `/v2/*` + 长轮询（`afterId + waitMs`）作为跨端最小基线；WebSocket 不作为当前强依赖。
- `GET /v2/logs` 新增 `format=text` 作为可读输出选项，不改变既有 `json/ndjson` 行为。
- SDK 统一策略升级为“默认内存 + 可选持久化后端”，在不破坏既有 API 的前提下满足本地先落盘诉求。
- v2 主契约已恢复为 `HTTP 查询 + WS 实时/下发双通道`，其中 `GET /v2/ws` 作为必选主入口，消息族覆盖 `hello`、`heartbeat`、`command.send`、`dispatch`、`ack`、`event.command_ack`、`event.command_summary`、`error`。
- 父仓新增 GitHub Actions 集成 CI 入口，统一调用 `docs-linhay/scripts/run-all-checks.sh`，并保留 `workflow_dispatch` 的 Harmony 构建开关。
- 父仓统一发布编排采用 `workflow_dispatch` + 跨仓 token 触发子仓 `workflow_dispatch`，避免在父仓重复实现发布构建链路。
- 集成测试节奏明确：优先本地三端 demo smoke，再跑父仓总门禁，最后进入 release orchestrator preflight。
- 集成测试策略固化：CI 默认跑核心测试，smoke 冒烟通过 workflow_dispatch 开关按需开启，发布前 preflight 默认开启 Harmony build gate。
- 模拟器执行策略补充：Android 采用本机 AVD 自动化落地；Harmony 先完成安装链路，启动阶段受目标设备锁屏策略约束；iOS 依赖本机 runtime 完整性。
- demo 策略固化：SDK 仓保留 CLI smoke，同时补充“可安装运行”的真实 demo app/entry 工程，作为模拟器接入基线。

## 风险与后续

- gateway 仍存在既有 warning（Vapor deprecate、并发捕获），当前不阻塞交付，后续可单开修复轮次。
- 当前契约已恢复 `/v2/ws`，但运行态实现仍需在各子仓完成 WS 侧对接。
- Harmony 当前已具备最小可构建 HAR 工程链路，剩余仅为签名警告与 ArkTS 可抛异常警告（不阻塞构建）。
- CLI + Web 主链路进入持续集成态：本地总门禁 + 各子仓 GitHub Actions 双重兜底。
- 子仓 CI 覆盖已扩展到 desktop / harmony / contracts，形成完整仓群门禁闭环。
- `docs-linhay/scripts/run-all-checks.sh` 增加对 Harmony 新同步脚本 `scripts/sync-harmony-module.sh` 的条件式 `sh -n` 最小校验，避免在子仓未 bump 时误报。
- 第十四轮并行最终收口：`neptune-desktop-macos@4424598`（打包脚本）、`neptune-inspector-h5@1ae2c66`（desktop 产物流水线）、`neptune-gateway-swift@e00f74e`（CLI release 构建脚本 + sha256 + 测试）。
- 父仓门禁脚本切换为“优先 `xcrun swift`，回退 `swift`”，解决本机 `swiftly` 未选中默认 toolchain 导致的误失败。
- 第十五轮并行收口：`neptune-gateway-swift@ae60d3b`（手动 `Release CLI` workflow，上传 `dist/cli-release`）、`neptune-desktop-macos@1c4536d`（手动 desktop 打包 workflow，联动 inspector 资源构建并上传 app artifact）。
- 第十六轮并行预留：`neptune-gateway-swift` 对应 gateway tag release，`neptune-desktop-macos` 对应 desktop zip+release；父仓仅补 workflow YAML 语法校验，不引入网络或子仓逻辑改动。
- 第十七轮预留：父仓新增统一发布编排入口，后续只通过 workflow_dispatch API 触发子仓 release workflow；该流程需要跨仓 PAT/机器人 token，`GITHUB_TOKEN` 不作为默认能力假设。
- 新决策：发布编排入口默认先跑 preflight 集成测试（含 Harmony build gate），通过后才触发 gateway/desktop release 分发；并将 `release-cli-tag.yml`、`release-desktop.yml` 纳入父仓 YAML 语法校验。
- demo 集成启动：新增 `neptune-sdk-web/examples/smoke-demo/run.cjs` 与父仓脚本 `docs-linhay/scripts/smoke-demo-web.sh`，用于本地一键验证 `Web SDK -> Gateway -> /v2/logs|/v2/sources|/v2/metrics` 链路。
- demo 冒烟实测通过：`smoke-demo-web.sh` 拉起本地 gateway 后，Web demo 成功写入并查回 3 条日志，`/v2/sources` 返回 1 个来源，`/v2/metrics` 指标可读。
- Android demo 曾出现 Kotlin plugin 多次加载 warning（`:sdk` 与 `:examples:smoke-demo`），已在后续提交通过根工程插件预加载方式解决。
- Android Kotlin plugin 多重加载 warning 已清零（`neptune-sdk-android@ddfbf59`）；全量门禁下 `./gradlew test` 与 `./gradlew smokeDemo` 稳定通过。
- Harmony 当前主要阻塞为设备锁屏策略（`10106102`）而非构建/安装问题；需手动解锁目标设备后再执行 `aa start`。
- iOS 当前主要阻塞为本机 Simulator runtime 缺失（`simctl list runtimes` 返回空），需安装可用 iOS runtime 后再进行启动验证。
- Android/Harmony 运行态阻塞：当前执行机缺 Android 标准 SDK（`adb`/`emulator`/`sdkmanager` 不可用），且 Harmony `hdc list targets` 为空；因此两端仅完成构建级验证，未完成“连接模拟器后 install/start”动作。

## 追加进展（第二十三轮）

- Harmony 运行态阻塞已清零：
  - `neptune-sdk-harmony` 子仓新增提交 `f7a1679`（`fix: export entry ability for simulator launch`）
  - 关键改动：`entry/src/main/module.json5` 为 `EntryAbility` 补充 `exported: true`
  - 验证结果：`hdc shell aa start -b io.github.neptune.sdk.harmony -m entry -a EntryAbility` 返回 `start ability successfully`
- 证据补充：
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-demo-after-v02.png`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-demo-after-v03.png`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-demo-after-v05.jpeg`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-entry-layout-after-v02.json`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-entry-layout-after-v03.json`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-entry-layout-after-v05.json`
- iOS 当前阻塞保持不变：
  - `xcrun simctl list runtimes` 仍为空，待 runtime 下载恢复后继续 simulator 冒烟。

## 追加进展（第二十四轮）

- 新增父仓二端编排脚本：`docs-linhay/scripts/smoke-demo-android-harmony.sh`
  - 默认执行 Android/Harmony 协议 smoke：`./gradlew smokeDemo` + `node ./scripts/demo-smoke.mjs`
  - 可选开关：
    - `NEPTUNE_CHECK_ANDROID_SIM=1`：追加 Android 模拟器安装/启动校验
    - `NEPTUNE_CHECK_HARMONY_SIM=1`：追加 Harmony `aa start` 校验
- 已完成验证：
  - `bash docs-linhay/scripts/smoke-demo-android-harmony.sh` 通过
  - `NEPTUNE_CHECK_HARMONY_SIM=1 bash docs-linhay/scripts/smoke-demo-android-harmony.sh` 通过
- Harmony 新证据：
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-demo-after-v06.jpeg`
  - `docs-linhay/screenshots/20260324/harmony/20260324-harmony-entry-layout-after-v06.json`
- Android 当前环境结论：
  - 协议 smoke 通过，SDK 主链路可用
  - 实机安装链路受 `adb devices` 在线状态抖动影响（离线设备残留），已作为可选模拟器校验处理，不阻塞两端协议推进

## 追加进展（第二十五轮）

- 日志契约同步完成：
  - `docs-linhay/api/openapi.yaml` 已与 `neptune-contracts/openapi/openapi.yaml` 对齐到完整 v2 日志契约（不再是精简占位版）
- 新增日志契约防漂移脚本：
  - `docs-linhay/scripts/check-log-contract-parity.sh`
  - 校验点：必含 6 个日志接口、禁止 `/v2/ws`、父仓与 contracts OpenAPI 文件一致
- 新增日志专用门禁：
  - `docs-linhay/scripts/run-log-checks.sh`
  - 覆盖 contracts/gateway/sdk-ios/sdk-android/sdk-harmony/sdk-web/inspector-h5
- 已验证通过：
  - `bash docs-linhay/scripts/check-log-contract-parity.sh`
  - `bash docs-linhay/scripts/run-log-checks.sh`（全链路 green）

## 追加进展（第二十六轮）

- 空白页根因已定位并修复：
  - 根因：desktop 打包后的 `index.html` 使用绝对资源路径 `/assets/...`，`file://` 场景下解析失败导致窗口空白
  - 修复：`neptune-inspector-h5/scripts/build-desktop-assets.sh` 改为 `npm run build -- --base ./`
  - 子仓提交：`neptune-inspector-h5@6dfdb18`
- iOS 实机 simulator 冒烟恢复：
  - 可用 runtime：`iOS 26.3 (26.3.1 - 23D8133)`
  - 通过命令：`NEPTUNE_DEMO_SIMULATOR_ID=1126FA83-5C54-4803-ABC4-FBBE05A9FCDD bash scripts/simulator-demo.sh`
  - 关键证据：`** BUILD SUCCEEDED **`，`simctl launch` 成功（`com.neptunekit.demo.ios: 19782`）
- desktop 联调补充：
  - 以 `NEPTUNE_GATEWAY_BIN=/Users/linhey/Desktop/linhay-open-sources/NeptuneKit/neptune-gateway-swift/.build/arm64-apple-macosx/release/neptune-gateway` 启动 app 后，`/v2/health` 与 `/v2/logs` 可访问。

## 追加进展（第二十七轮）

- 新增并行冒烟编排脚本：
  - `docs-linhay/scripts/smoke-parallel-clients-desktop.sh`
  - 并行执行三条线：`native`（iOS/Android/Harmony）、`web`、`desktop`
  - 日志输出固定到 `.build/smoke-logs/*.log`
- 并行冒烟实测通过（同一次执行）：
  - `[PASS] native`
  - `[PASS] web`
  - `[PASS] desktop`
  - `[parallel-smoke] all smoke jobs passed`
- 关键结论：
  - 多客户端与 desktop 并行推进链路已具备一键回归入口，且当前全绿，无新增阻塞。
- Android discovery 能力已落地：
  - `neptune-sdk-android` 新增 `discovery` 模块，使用 `JmDNS 3.6.3` 做 mDNS 候选发现，HTTP 探测使用内置 `HttpURLConnection`，并支持 `manualDsn` 回退。
  - `./gradlew :sdk:test` 已通过，单测覆盖了 mDNS 优先、手动 DSN 回退、非法 discovery 响应跳过和全失败语义。

## 追加进展（第二十八轮）

- 发现职责链路已冻结并写入 spec：
  - 顺序固定为 `CLI 提供发现服务 -> App SDK 发现 CLI -> App SDK 上报日志 -> H5 仅连 CLI 展示`
  - 明确否定了“App 注册发现服务”的职责模型。
- 三端 demo 已接入 discovery 入口并可见结果：
  - iOS simulator：新增 `Discover Gateway` 按钮，页面日志展示 `source/host/port/version/endpoint`。
  - Android simulator：新增“发现网关”按钮；补 `android:usesCleartextTraffic=true`；默认 DSN 修正为 `10.0.2.2:18765`。
  - Harmony entry：新增“发现网关”按钮与发现状态卡片。
- 关键验证：
  - iOS：`xcrun swift test -q`、`xcodebuild test ... SimulatorApp` 通过。
  - Android：`:sdk:test`、`:app:testDebugUnitTest`、模拟器点击后 `status=ok`（logcat 与 UI 均可见）。
  - Harmony：`verify-gateway-discovery.mjs`、`verify-demo-entry.mjs`、`assembleHap` 通过。

## 追加进展（第二十九轮）

- Desktop 固定启动链路已确认可用：
  - 执行 `bash docs-linhay/scripts/start-desktop-fixed.sh`
  - 结果包含：`window count: 1`、`gateway health: {"status":"ok","version":"2.0.0-alpha.1"}`
- 三端真实模拟器冒烟闭环完成（发现 -> ingest -> 网关可见）：
  - iOS：通过 `SIMCTL_CHILD_NEPTUNE_DEMO_AUTO_DISCOVER=1 xcrun simctl launch ...` 自动触发 discovery+ingest，`/v2/sources` 出现 `platform=ios`。
  - Android：模拟器点击“发现网关”后，logcat 显示
    - `discoverGateway status=ok source=manual-dsn ...`
    - `ingestGateway status=ok endpoint=http://10.0.2.2:18765/v2/logs:ingest responseCode=202`
    `/v2/sources` 出现 `platform=android`。
  - Harmony：
    - 先恢复 `hdc` 连接（`hdc start` 后 `hdc list targets -> 127.0.0.1:5555`）
    - 重新 `assembleHap + hdc install -r + aa start`
    - `/v2/sources` 出现 `platform=harmony`。
- Harmony 模拟器网络关键修正（避免 discovery 成功但 ingest 失败）：
  - `entry/src/main/ets/runtime/DemoRuntime.ets`
    - 手动 DSN 从 `127.0.0.1:18765` 调整为 `10.0.2.2:18765`
    - 当 `source=manual-dsn` 时，ingest endpoint 强制使用 `http://10.0.2.2:18765`
  - `entry/src/main/ets/pages/Index.ets` 文案同步为 `10.0.2.2:18765`
- 汇总验收：
  - `bash docs-linhay/scripts/check-gateway-platform-sources.sh` 通过
  - 输出：`sources.platforms=android,harmony,ios`。

## 追加进展（第三十轮）

- Desktop 出现 `partial refresh | logs/sources/metrics: Load failed` 的根因已确认：
  - H5 Inspector 以独立 origin（`http://127.0.0.1:4173`）访问 CLI 网关（`http://127.0.0.1:18765`）
  - 网关此前未返回 CORS 头，WKWebView/浏览器会把请求判定为跨域失败。
- 修复：
  - 在 `neptune-gateway-swift` 增加全局 `CORSMiddleware`（允许 `GET/POST/OPTIONS`，允许 `origin/content-type/...` 头）。
  - 新增测试 `testMetricsEndpointIncludesCORSHeadersForInspectorOrigin`，防止回归。
- 验证：
  - `xcrun swift test -q` 全通过（新增 CORS 测试通过）。
  - `OPTIONS /v2/logs` 返回 `200`，且响应包含 `access-control-allow-origin: *`。
- 三端可见状态恢复：
  - 重新触发 iOS / Android / Harmony discovery+ingest 后，`/v2/sources` 为 `android,harmony,ios`，`sourceCount=3`。

## 追加进展（第三十一轮）

- iOS SDK 新增 WS 客户端：
  - 基于 `URLSessionWebSocketTask`，启动即连 discovery endpoint 对应的 `/v2/ws`
  - 首包发送 `hello(role=sdk)`，并按 `15s` 心跳、`45s` 失联判定、`0.5/1/2/4/8s` 退避重连
  - 维护循环会周期性复查 discovery；endpoint 变化时自动重连到新地址
  - 收到 `command.dispatch(ping)` 时立即回 `command.ack(status=ok,timestamp)`，demo 输出可见
- 验证结果：
  - `swift test --filter GatewayWebSocketClientTests --filter SimulatorAppTests` 通过
  - `swift test` 全量通过（20 tests / 6 suites）

## 第三十二轮进展（WS 下发主链路对齐）

- 已完成 `/v2/ws` 协议对齐并跨仓落地：
  - Gateway：`command.send -> ack -> command.dispatch -> command.ack -> event.command_ack -> event.command_summary`，并保留 `event.log_record(topicId=101)`
  - Inspector：启动即连、15s 心跳、0.5/1/2/4/8s 重连、target 校验、ACK/summary 展示
  - iOS/Android/Harmony：启动即连、ping 回 ACK、discovery 端点变化重连
- 契约与门禁已切换：
  - `neptune-contracts/openapi/openapi.yaml` 与 `docs-linhay/api/openapi.yaml` 同步恢复 `/v2/ws`
  - `check-log-contract-parity.sh` 改为“必须包含 `/v2/ws`”
- 回归验证：
  - `swift test`（gateway）
  - `xcrun swift test`（ios）
  - `./gradlew test`（android）
  - `npx vitest run && npm run build`（inspector）
  - `node scripts/verify-gateway-ws-contract.mjs && assembleHar`（harmony）
  - `bash docs-linhay/scripts/smoke-parallel-clients-desktop.sh`（native/web/desktop 全 PASS）

## 第三十三轮进展（WS 专项门禁补齐）

- 新增脚本：`docs-linhay/scripts/run-ws-checks.sh`
  - 聚焦 WS 下发链路最小闭环：gateway + inspector + ios + android + harmony
- 新增脚本：`docs-linhay/scripts/smoke-ws-live.sh`
  - 自启 gateway（默认 `19001`）
  - 拉起 3 个模拟 sdk（ios/android/harmony）+ 1 个 inspector
  - 实测验证 `command.send -> ack -> event.command_ack -> event.command_summary`
- 关键验证结果：
  - `bash docs-linhay/scripts/run-ws-checks.sh` 通过
  - `bash docs-linhay/scripts/smoke-ws-live.sh` 通过
  - 实测输出：`acceptedAck.delivered=2`，`summary.acked=2`，`summary.timeout=0`
- 门禁编排增强：
  - `run-all-checks.sh` 新增可选开关：`NEPTUNE_CHECK_WS_SMOKE=1`、`NEPTUNE_CHECK_WS_LIVE_SMOKE=1`

## 第三十四轮进展（WS 定向命中修复 + 三端联调）

- 根因定位：Gateway 按 `target(platform/app/session/device)` 做精确定向时，三端 SDK 的 `hello` 只发了 `role=sdk`，未带身份字段，导致 `command.send(ping)` 出现 `delivered=0`。
- 并行修复已落地：
  - iOS：`NeptuneGatewayWebSocketConfiguration` 增加 `platform/appId/sessionId/deviceId`，`hello` 改为携带身份字段。
  - Android：`GatewayWebSocketClient` 增加身份参数（含默认值），`hello` 带齐四字段；模拟器 Demo 显式传入 identity。
  - Harmony：`GatewayWsClientOptions` 增加身份参数，`hello` 带齐四字段；DemoRuntime 显式注入 identity。
- 相关验证：
  - iOS：`swift test --filter GatewayWebSocketClientTests` 通过（6 tests）
  - Android：`./gradlew :sdk:test --tests com.neptunekit.sdk.android.ws.GatewayWebSocketClientTest` 通过
  - Harmony：`node scripts/verify-gateway-ws-contract.mjs` 通过
- 三端实机联调结果：
  - 初次复测：`delivered=2, acked=2`（iOS+Android）
  - Harmony 追加网络桥接：`hdc rport tcp:18765 tcp:18765`
  - 复测达成：`delivered=3, acked=3, timeout=0`，ACK 客户端为 `ios/android/harmony`
- 截图归档：
  - iOS：`docs-linhay/screenshots/20260324/ios/20260324-ios-simulator-ws-ack-after-v01.png`
  - Android：`docs-linhay/screenshots/20260324/android/20260324-android-simulator-ws-ack-after-v01.png`
  - Harmony：`docs-linhay/screenshots/20260324/harmony/20260324-harmony-simulator-ws-ack-after-v12.jpeg`
  - Desktop：当前 app 窗口仍存在空白页问题（截图 `docs-linhay/screenshots/20260324/desktop/20260324-desktop-inspector-command-ack-after-v04.png`），需单独处理 WKWebView 加载链路。
