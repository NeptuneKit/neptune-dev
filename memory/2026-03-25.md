# 2026-03-25

## 关键决策
- 统一 discovery endpoint 解析策略：当 `/v2/gateway/discovery` 返回 `host` 为 loopback/wildcard（`127.0.0.1`、`localhost`、`::1`、`::`、`0.0.0.0`）时，SDK 侧后续连接地址优先回退到当前命中的候选 host（mDNS/manual DSN）。
- 该策略已在 iOS、Android、Harmony 三端同步，避免模拟器环境把 gateway 返回的 loopback 当成真实可达地址，导致 WS/ingest 断链。

## 本次验证
- Android：`GatewayDiscoveryTest` 新增 loopback 回退用例并通过。
- iOS：`GatewayDiscoveryTests` 新增 loopback 回退用例并通过。
- Harmony：`verify-gateway-discovery.mjs` 增加 loopback 回退校验并通过。
- Harmony 历史冒烟日志确认可命中 `source=mdns`；同时补充 `GatewayWsManager` discovery 失败时输出 `attempts`，便于继续定位设备侧网络差异。

## 三端联调结果
- 2026-03-25 01:05（Asia/Shanghai）完成 iOS/Android/Harmony 三端模拟器联调，网关 `ws://127.0.0.1:18765/v2/ws` 下发 `ping` 定向测试结果均为 `delivered=1, acked=1, timeout=0`。
- iOS 客户端：`com.neptunekit.demo.ios`（iOS Simulator）命中并回 ACK。
- Android 客户端：`com.neptunekit.sdk.android.examples.simulator`（emulator-5556）命中并回 ACK。
- Harmony 客户端：`io.github.neptune.sdk.harmony.demo`（127.0.0.1:5555）命中并回 ACK。
- 网关稳定运行方式：以前台会话常驻 `neptune-gateway serve --host 0.0.0.0 --port 18765 --mdns --mdns-service-name neptune-gateway`，避免被并发任务误杀造成 `ECONNREFUSED`。

## iOS SDK 主链路切换
- 2026-03-25 完成 `neptune-sdk-ios` 的主动回调 HTTP 链路改造：本地 HTTP 服务默认绑定 `0.0.0.0`，新增 `POST /v2/client/command` 与 `POST /v1/client/command`，并新增 `NeptuneGatewayRegistrationClient` 对网关 `POST /v2/clients:register` 做启动即时注册与 30s 续约。
- 目标主键按 `platform + appId + deviceId` 组织，`sessionId` 仅保留用于展示字段。
- 已补充测试覆盖：command ping ACK、register payload、续约节拍；`swift test` 通过。

## Gateway 主链路切换（主动回调）
- 2026-03-25 网关新增三条主接口：`POST /v2/clients:register`、`GET /v2/clients`、`PUT /v2/clients:selected`，并将命令执行从 SDK WS 下发切换为“对 selected+online 客户端并发 HTTP 回调 `POST <callbackEndpoint>/v2/client/command`（3s timeout）”。
- WS 仍保留 Inspector 通道：`ack`、`event.command_ack`、`event.command_summary`、`event.log_record(topicId=101)`；SDK 不再依赖默认 WS 主链路。
- 网关在线状态改为注册心跳模型：SDK 30s 续约、网关 120s TTL 过期下线；CORS 已放开 `PUT` 以支持 `clients:selected`。
- `neptune-gateway-swift` 回归通过：`swift test`（42 tests, 1 skipped）。

## 参考项目登记
- 2026-03-25：将 `LookInside`（<https://github.com/Lakr233/LookInside>）登记为 NeptuneKit 参考项目；索引文档：`docs-linhay/references/README.md`。
- 2026-03-25：已完成仓库拉取，落地路径 `docs-linhay/references/LookInside`，用于后续参考对标。
